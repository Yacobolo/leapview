apiVersion: libredash.dev/v1
kind: Project
metadata:
  name: libredash-showcase
spec:
  connections:
    include:
      - connections/*.yaml
  sources:
    include:
      - sources/*.yaml
  workspaces:
    include:
      - workspaces/*/workspace.yaml
