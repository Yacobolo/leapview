apiVersion: leapview.dev/v1
kind: Workspace
metadata:
  name: visual_examples
  title: Visual documentation examples
spec:
  uses:
    sources:
    - docs.orders
    - docs.conversion_stages
    - docs.revenue_bridge
    - docs.market_ohlc
    - docs.service_teams
  models:
    include:
    - models/*.yaml
  semanticModels:
    include:
    - semantic-models/*.yaml
  dashboards:
    include:
    - dashboards/*.yaml
  access:
    include:
    - access/*.yaml
