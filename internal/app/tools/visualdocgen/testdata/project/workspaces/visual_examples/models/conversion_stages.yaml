apiVersion: leapview.dev/v1
kind: ModelTable
metadata:
  workspace: visual_examples
  name: conversion_stages
  title: Conversion stages
spec:
  primaryKey: stage
  sources: [docs.conversion_stages]
  fields:
    stage: {label: Stage, type: string}
    conversions: {label: Conversions, type: number}
  transform:
    sql: |
      SELECT stage, CAST(conversions AS DOUBLE) AS conversions
      FROM source."docs.conversion_stages"
