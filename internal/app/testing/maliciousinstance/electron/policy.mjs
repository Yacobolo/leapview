// The malicious-instance harness exercises the exact policy shipped by the
// desktop application. Keep this compatibility import so existing proof
// commands and hosted jobs retain stable paths.
export * from "../../../../../desktop/src/security/remote-policy.mjs";
