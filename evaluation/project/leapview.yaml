apiVersion: leapview.dev/v1
kind: Project
metadata:
  name: leapview-evaluation
spec:
  connections:
    include:
      - connections/*.yaml
  sources:
    include:
      - sources/*.yaml
  workspaces:
    include:
      - workspaces/*/workspace.yaml
